// for (let step = 0; step < 5; step++) {
//   console.log(`step ${step}`)
// }

function charCount(str: string) {
  const result = {}

  console.log(str)

  // for (let i )

}

const args = process.argv.slice(2) || 'hi'
charCount(args[0])
